generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model product {
  id      String @id @default(auto()) @map("_id") @db.ObjectId
  cost    Float
  picture String
  title   String
}
